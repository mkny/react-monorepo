{
  "name": "monorepo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "@babel/preset-typescript": "^7.18.6",
    "lerna": "^6.4.1",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "lint": "lerna run lint --parallel",
    "build:libs": "lerna run build:watch --parallel",
    "dev": "concurrently npm:build:libs \"npm:web start\" -k",
    "web": "npm run -w @monorepo/web"
  },
  "dependencies": {
    "concurrently": "^8.0.1"
  }
}
